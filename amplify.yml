version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --prefix cascade
    build:
      commands:
        - npm run build --prefix cascade
  artifacts:
    baseDirectory: cascade/dist
    files:
      - '**/*'
  cache:
    paths:
      - cascade/node_modules/**/*

